# Chapter 22: Bayesian Computation in Julia with Turing.jl

## Introduction

Turing.jl is Julia's premier probabilistic programming framework, providing an expressive @model macro for specifying Bayesian models, multiple automatic differentiation backends for efficient gradient-based sampling, and seamless integration with Julia's optimization and distribution ecosystems. This chapter implements every Bayesian method from Chapter 15 in Turing.jl, providing production-ready code for Bayesian regression, hierarchical models for provider profiling, cost-effectiveness analysis, network meta-analysis, and spatial models.

The combination of Turing.jl's expressiveness and Julia's computational performance creates a uniquely powerful environment for Bayesian health economics: models that would require hours to sample in JAGS or WinBUGS complete in minutes, and the resulting posterior samples can be immediately analyzed, visualized, and fed into decision-analytic frameworks using Julia's statistical and plotting ecosystem.

## Turing.jl Architecture

### The @model Macro

Turing.jl models are defined using the @model macro, which transforms a Julia function into a probabilistic model by interpreting the ~ operator as a distributional statement:

@model function linear_regression(X, y)
    n, p = size(X)
    β ~ MvNormal(zeros(p), 10.0 * I)
    σ ~ truncated(Normal(0, 5), 0, Inf)
    μ = X * β
    for i in 1:n
        y[i] ~ Normal(μ[i], σ)
    end
end

The ~ operator serves dual purposes: for parameters (left-hand side not observed), it defines a prior distribution; for data (left-hand side observed), it defines the likelihood contribution. Turing.jl automatically constructs the log-posterior from these statements and provides the gradient (via automatic differentiation) needed for HMC/NUTS sampling.

### Sampling and Inference

Sampling uses the NUTS algorithm by default:

model = linear_regression(X, y)
chain = sample(model, NUTS(0.65), 2000)

The 0.65 argument is the target acceptance rate (0.65 is the default; higher values produce shorter steps with higher acceptance but slower exploration). The warmup period (default: half the requested samples) adapts the step size and mass matrix for efficient sampling.

Multiple chains for convergence assessment are run in parallel:

chains = sample(model, NUTS(), MCMCThreads(), 2000, 4)

This runs 4 independent chains of 2000 samples each, enabling R̂ computation and between-chain variance assessment. MCMCThreads() distributes chains across Julia threads; MCMCDistributed() distributes across processes for models where thread-safety is a concern.

### Convergence Diagnostics

Turing.jl integrates with MCMCDiagnosticTools.jl for convergence assessment:

summarystats(chain)  # R̂, ESS, mean, std, quantiles for each parameter
ess(chain)           # effective sample size
rhat(chain)          # potential scale reduction factor

Trace plots and autocorrelation plots are generated with StatsPlots.jl: plot(chain) produces a multi-panel display of trace plots and marginal posterior density plots for all parameters. Visual inspection of trace plots — looking for the "hairy caterpillar" pattern of good mixing and stationarity — supplements the quantitative diagnostics.

## Bayesian Hierarchical Models for Hospital Quality

### Hospital Mortality Profiling

The hierarchical model for hospital quality profiling shrinks hospital-specific estimates toward the population mean, preventing unreliable extreme estimates for small-volume hospitals:

@model function hospital_mortality(y, X, hospital_id, n_hospitals)
    p = size(X, 2)
    β ~ MvNormal(zeros(p), 10.0 * I)
    τ ~ truncated(Cauchy(0, 1), 0, Inf)
    
    α = Vector{Float64}(undef, n_hospitals)
    for j in 1:n_hospitals
        α[j] ~ Normal(0, τ)
    end
    
    for i in eachindex(y)
        logit_p = dot(X[i,:], β) + α[hospital_id[i]]
        y[i] ~ BernoulliLogit(logit_p)
    end
end

The hierarchical prior α[j] ~ Normal(0, τ) induces shrinkage: hospitals with few patients are pulled heavily toward the overall mean (α = 0), while large-volume hospitals retain estimates close to their MLE. The hyperparameter τ (the between-hospital standard deviation) is estimated from the data and controls the degree of shrinkage.

The posterior distribution of each α[j] provides a credible interval for hospital j's quality effect. Hospitals whose credible intervals exclude zero can be classified as significantly above or below average with quantified uncertainty — the foundation of CMS's star rating methodology.

## Bayesian Cost-Effectiveness Analysis

### Joint Cost-Effect Modeling

Bayesian CEA models costs and effects jointly, propagating parameter uncertainty through to the decision:

@model function bayesian_cea(costs_new, costs_std, effects_new, effects_std)
    μ_c_new ~ Normal(50000, 10000)
    μ_c_std ~ Normal(45000, 10000)
    μ_e_new ~ Normal(8.5, 1.0)
    μ_e_std ~ Normal(8.0, 1.0)
    σ_c ~ truncated(Normal(0, 5000), 0, Inf)
    σ_e ~ truncated(Normal(0, 0.5), 0, Inf)
    
    for i in eachindex(costs_new)
        costs_new[i] ~ Normal(μ_c_new, σ_c)
        effects_new[i] ~ Normal(μ_e_new, σ_e)
    end
    for i in eachindex(costs_std)
        costs_std[i] ~ Normal(μ_c_std, σ_c)
        effects_std[i] ~ Normal(μ_e_std, σ_e)
    end
end

From the posterior samples, compute incremental costs and effects:

ΔC = chain[:μ_c_new] .- chain[:μ_c_std]
ΔE = chain[:μ_e_new] .- chain[:μ_e_std]

The CEAC is constructed by computing net benefit at each WTP threshold:

λ_grid = 0:1000:200000
ceac = [mean(λ .* ΔE .- ΔC .> 0) for λ in λ_grid]

EVPI is computed as:

optimal_per_draw = max.(λ .* ΔE .- ΔC, 0)
evpi = mean(optimal_per_draw) - max(mean(λ .* ΔE .- ΔC), 0)

## Bayesian Network Meta-Analysis

NMA in Turing.jl models the treatment effects across a network of trials with random effects for between-study heterogeneity:

@model function nma(y, se, treat_a, treat_b, n_studies, n_treats)
    d = Vector{Float64}(undef, n_treats)
    d[1] = 0.0  # reference treatment
    for k in 2:n_treats
        d[k] ~ Normal(0, 10)
    end
    τ ~ truncated(Cauchy(0, 0.5), 0, Inf)
    
    for k in 1:n_studies
        δ ~ Normal(d[treat_b[k]] - d[treat_a[k]], τ)
        y[k] ~ Normal(δ, se[k])
    end
end

Treatment rankings are computed from the posterior: for each MCMC draw, rank treatments by their estimated effect d[k], then compute the probability that each treatment is best (rank 1) and the SUCRA score.

## Model Comparison

Bayesian model comparison uses WAIC and LOO-CV via ParetoSmooth.jl:

using ParetoSmooth
waic_result = waic(chain, model)
loo_result = loo(chain, model)

Lower WAIC/LOO-IC indicates better out-of-sample predictive performance. The Pareto k diagnostic identifies observations with high influence on the LOO estimate — values above 0.7 suggest unreliable LOO estimates for those observations.

Posterior predictive checks generate simulated datasets from the posterior and compare their statistical properties to the observed data, assessing whether the model can reproduce the salient features of the data (distributional shape, tail behavior, key summary statistics).

---

**Table 22.1: Turing.jl Implementation Guide**

| Model Type | Key Turing.jl Features | Sampler | Typical ESS/sec |
|---|---|---|---|
| Linear regression | MvNormal prior, Normal likelihood | NUTS | 1000+ |
| Logistic regression | BernoulliLogit, vectorized | NUTS | 500-1000 |
| Hierarchical (hospital profiling) | Random effects + hyperpriors | NUTS | 200-500 |
| Cost-effectiveness | Joint cost-effect, net benefit | NUTS | 500+ |
| Network meta-analysis | Multi-study random effects | NUTS | 300-500 |
| Spatial (BYM) | CAR prior via sparse precision | NUTS | 50-200 |

---

## Conclusion

Turing.jl provides a powerful, expressive, and computationally efficient platform for Bayesian health economics analysis. The @model macro makes model specification intuitive, the NUTS sampler handles complex posteriors without manual tuning, and Julia's performance ensures that even large hierarchical models sample in reasonable time. The integration with Julia's broader ecosystem — Distributions.jl for priors, Makie.jl for posterior visualization, ParetoSmooth.jl for model comparison — creates a complete Bayesian analysis workflow within a single language.

## References

1. Ge, H., Xu, K., & Ghahramani, Z. (2018). Turing: A language for flexible probabilistic inference. *AISTATS*.
2. Turing.jl documentation. https://turinglang.org
3. ParetoSmooth.jl. https://github.com/TuringLang/ParetoSmooth.jl
4. Vehtari, A., Gelman, A., & Gabry, J. (2017). Practical Bayesian model evaluation using leave-one-out cross-validation and WAIC. *Statistics and Computing*, 27(5), 1413-1432.

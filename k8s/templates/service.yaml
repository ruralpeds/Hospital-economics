apiVersion: v1
kind: Service
metadata:
  name: {{ include "rhsim.fullname" . }}
  labels:
    {{- include "rhsim.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "rhsim.selectorLabels" . | nindent 4 }}
